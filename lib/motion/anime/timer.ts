/**
 * anime's Timer — a callback on anime's own animation frame (no extra rAF
 * loop). Used for the scrub glide and the Core's canvas. See ./core.ts.
 */
export { createTimer } from "animejs/timer";
export type { Timer } from "animejs/timer";
